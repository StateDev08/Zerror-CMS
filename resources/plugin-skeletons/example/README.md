# Example Plugin

Skeleton für den ZerroCMS ZIP-Installer.

- `plugin.json` – Manifest (`name` = Ordner-/DB-Key)
- `PluginServiceProvider.php` – muss den Provider-Klassennamen `return`en

Packe den Ordnerinhalt als ZIP und lade ihn unter **System → Plugins** hoch.
