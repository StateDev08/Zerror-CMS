# Beispiel-Widget-Paket

ZIP-Struktur (Ordner `example/` im Archiv):

- `widget.json` – Manifest (`id`, `name`, `version`, `slots`)
- `Widget.php` – gibt eine `WidgetContract`-Instanz (oder Klassenname) zurück

Installieren über ACP → System → Paket-Installer.
